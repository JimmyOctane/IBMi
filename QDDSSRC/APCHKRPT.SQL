CREATE OR REPLACE TABLE HD1100PD.APCHKRPT (
  CHECK_NUM     DECIMAL(7, 0) NOT NULL,
  CHECK_DATE    INTEGER       NOT NULL,
  VENDOR_NUM    DECIMAL(6, 0) NOT NULL,
  VENDOR_NAME   CHAR(30)      NOT NULL,
  AMOUNT        DECIMAL(11, 2) NOT NULL
);
